version: 2

sources:
  - name: jaffle_shop
    tables:
      - name: orders
      - name: customers
        description: Raw Orders data
        columns:
          - name: id
            description: Primary Key for Orders
            tests:
              - unique
              - not_null
      - name: mock_orders
        description: mock orders raw data
